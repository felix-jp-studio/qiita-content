---
title: "Firebase×Next.jsで認証機能を30分で実装する手順"
tags:
  - Next.js
  - Firebase
  - TypeScript
  - 初心者
private: false
updated_at: ''
id: null
organization_url_name: null
slide: false
ignorePublish: false
---

# Firebase×Next.jsで認証機能を30分で実装する手順

Webアプリケーションに認証機能を追加したいけれど、「難しそう...」と感じている初心者の方も多いのではないでしょうか？

実は、FirebaseとNext.jsを組み合わせれば、わずか30分程度でユーザー登録・ログイン機能を実装できます。この記事では、その手順をわかりやすく解説します。

## なぜFirebase Authentication？

Firebase Authenticationを使う理由は以下の通りです：

- **セキュリティが高い**：Googleが提供する信頼性の高い認証システム
- **実装が簡単**：複雑な認証ロジックを自作する必要がない
- **無料枠が充実**：小〜中規模のアプリなら無料で利用可能
- **多様な認証方法**：メール/パスワード、Google、Twitter等に対応

## 事前準備

��下の環境が必要です：

1. Node.js（v16以上）
2. Next.jsプロジェクト
3. Firebaseプロジェクト（Firebase Consoleで作成）

## 実装手順

### 1. 必要なパッケージをインストール

```bash
npm install firebase react-firebase-hooks
```

### 2. Firebase設定ファイルを作成

`lib/firebase.ts`を作成し、Firebase設定を記述します：

```typescript
import { initializeApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';

const firebaseConfig = {
  apiKey: process.env.NEXT_PUBLIC_FIREBASE_API_KEY,
  authDomain: process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,
  projectId: process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID,
  storageBucket: process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
  appId: process.env.NEXT_PUBLIC_FIREBASE_APP_ID
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
```

### 3. 環境変数を設定

`.env.local`ファイルにFirebaseの設定情報を記述します（Firebase Consoleから取得）。

### 4. 認証コンポーネントを作成

`components/Auth.tsx`を作成：

```typescript
import { useState } from 'react';
import { signInWithEmailAndPassword, createUserWithEmailAndPassword } from 'firebase/auth';
import { auth } from '../lib/firebase';

export default function Auth() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [isLogin, setIsLogin] = useState(true);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    try {
      if (isLogin) {
        await signInWithEmailAndPassword(auth, email, password);
      } else {
        await createUserWithEmailAndPassword(auth, email, password);
      }
      console.log('認証成功！');
    } catch (error) {
      console.error('認証エラー:', error);
    }
  };

  return (
    <div>
      <h2>{isLogin ? 'ログイン' : '新規登録'}</h2>
      <form onSubmit={handleSubmit}>
        <input
          type="email"
          placeholder="メールアドレス"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
        />
        <input
          type="password"
          placeholder="パスワード"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
        />
        <button type="submit">
          {isLogin ? 'ログイン' : '新規登録'}
        </button>
      </form>
      <button onClick={() => setIsLogin(!isLogin)}>
        {isLogin ? '新規登録はこちら' : 'ログインはこちら'}
      </button>
    </div>
  );
}
```

### 5. ユーザー状態を管理

`react-firebase-hooks`を使って、ユーザーのログイン状態を簡単に管理できます：

```typescript
import { useAuthState } from 'react-firebase-hooks/auth';
import { auth } from '../lib/firebase';

export default function Home() {
  const [user, loading, error] = useAuthState(auth);

  if (loading) return <div>読み込み中...</div>;
  if (error) return <div>エラーが発生しました</div>;

  return (
    <div>
      {user ? (
        <div>
          <p>ようこそ、{user.email}さん！</p>
          <button onClick={() => auth.signOut()}>ログアウト</button>
        </div>
      ) : (
        <Auth />
      )}
    </div>
  );
}
```

## まとめ

Firebase Authenticationを使えば、複雑な認証システムを短時間で実装できます。今回紹介